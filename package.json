{
  "name": "instagram",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install:client": "pnpm -C client install",
    "install:server": "pnpm -C server install",
    "install": "run-s install:client install:server",
    "dev:client": "pnpm -C client run dev",
    "dev:server": "pnpm -C server run dev",
    "dev": "run-p dev:client dev:server"
  },
  "keywords": [],
  "author": "natmfat",
  "license": "ISC",
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
